#ifndef KONUM_HPP
#define KONUM_HPP

class Konum {
public:
    int x; // Satır bilgisi
    int y; // Sütun bilgisi

    // Kurucu fonksiyon (Constructor)
    Konum(int x, int y);
    
    // Parametresiz kurucu (Gerekirse diye)
    Konum();
};

#endif